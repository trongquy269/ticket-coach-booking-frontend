import classNames from 'classnames/bind';

import styles from './ReportFile.module.scss';

const cx = classNames.bind(styles);

const ReportFile = () => {
	return <div className={cx('wrap')}>aaa</div>;
};

export default ReportFile;
